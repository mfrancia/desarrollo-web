<%= form_for(@raze) do |f| %>
  <% if @raze.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raze.errors.count, "error") %> prohibited this raze from being saved:</h2>

      <ul>
      <% @raze.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
